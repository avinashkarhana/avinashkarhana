# Hi there! I am `Avinash`
And I build and break stuff :)

## Go tests in Codespaces + browser MCP
- Open the Codespaces terminal.
- Run all tests: `go test -v ./...`
- Run one test by name: `go test -v ./... -run TestGoTestReadiness`
- Use browser MCP to drive/observe the Codespaces UI session while running commands.
